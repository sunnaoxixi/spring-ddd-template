- 所有子模块遵循 DDD 分层：client(application service 接口) / application(场景编排) / adaptor(HTTP 控制器) 三件套协作
- 跨层对象转换统一使用 MapStruct Assembler，禁止在 Service 中手写 getter/setter 映射
- 对外 API 返回统一 Result<T> 包装，异常通过全局处理器转换为标准错误响应
- 操作日志通过 @OperLog AOP 切面记录，登录/操作日志通过 Spring 事件异步持久化